{
  "name": "quicklink-logging-middleware",
  "version": "1.0.0",
  "description": "Reusable logging middleware for QuickLink application",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["logging", "middleware", "quicklink"],
  "author": "Student",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
